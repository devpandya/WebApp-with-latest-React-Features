import {GetResource, GetResourceString} from '../utility/AppContext'

export const GetResourceValue = (key) => { return GetResource(key)}

export const GetResourceText =   (key) => { return GetResourceString(key)}   